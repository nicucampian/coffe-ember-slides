{{#each layouts as |layout|}}
  {{#if (eq layout holdLayout) }}
    {{button-image imgUrl=layout.url click=(action 'clickPrototype' layout) class="layout-selected"}}
  {{else}}
    {{button-image imgUrl=layout.url click=(action 'clickPrototype' layout) }}
  {{/if}}
{{/each}}
{{yield}}
